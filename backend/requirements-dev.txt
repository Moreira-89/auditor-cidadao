# Ferramental de desenvolvimento local — documentação e testes. NÃO vai para a
# imagem Docker publicada (o Dockerfile só instala requirements.txt).

# Documentação técnica (MkDocs), ver docs/ e mkdocs.yml
mkdocs==1.6.1
mkdocs-material==9.7.6

# Testes
pytest==9.1.1
pytest-asyncio==1.4.0
